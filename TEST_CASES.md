# CodePulse 测试用例集

> 版本：v1.0  
> 日期：2026-07-05  
> 项目：CodePulse - 研发活跃度智能分析平台  
> 格式：Markdown  
> 覆盖范围：后端 API、前端交互、数据导入、统计计算、异常检测

---

## 1. 测试用例总览

| 模块 | 用例编号 | 用例数量 | 优先级 |
|------|----------|----------|--------|
| 数据导入 - Git Log 文件 | TC-GL-001 ~ TC-GL-006 | 6 | P0 |
| 数据导入 - JSON 粘贴 | TC-JS-001 ~ TC-JS-007 | 7 | P0 |
| 数据导入 - 仓库同步 | TC-SYNC-001 ~ TC-SYNC-005 | 5 | P0 |
| 数据库与去重 | TC-DB-001 ~ TC-DB-006 | 6 | P0 |
| 统计 API | TC-API-001 ~ TC-API-008 | 8 | P1 |
| 数据导出 | TC-EXP-001 ~ TC-EXP-003 | 3 | P1 |
| 前端交互 | TC-UI-001 ~ TC-UI-006 | 6 | P1 |
| 模拟数据 | TC-MOCK-001 ~ TC-MOCK-002 | 2 | P2 |
| **合计** | - | **46** | - |

---

## 2. 数据导入 - Git Log 文件测试用例

### TC-GL-001：标准 git log --stat 格式解析

| 项目 | 内容 |
|------|------|
| **用例名称** | 标准 git log --stat 格式文件上传 |
| **前置条件** | 1. 后端服务已启动<br>2. 数据库已清空（或测试隔离库） |
| **测试步骤** | 1. 准备标准 git log 文本文件：<br>`commit abc123...\nAuthor:张三 <zs@example.com>\nDate:...\n\nmessage\n\n1 file changed, 2 insertions(+), 1 deletions(-)`<br>2. 通过前端选择文件并点击"上传文件"<br>3. 或直接调用 `POST /api/commits/upload/git-log` |
| **预期结果** | 1. HTTP 200 返回 `{"message":"成功导入 1 条提交记录","count":1}`<br>2. 数据库中新增 1 条记录，commit_hash 为完整 40 位 |
| **优先级** | P0 |
| **执行状态** | ✅ 通过 |

### TC-GL-002：自定义 | 分隔格式解析

| 项目 | 内容 |
|------|------|
| **用例名称** | 自定义 hash\|author\|date\|message 格式上传 |
| **前置条件** | 同 TC-GL-001 |
| **测试步骤** | 1. 准备 git-log.txt，内容为：<br>`28c0e05 | yansonaa | 2026-07-05 07:48:16 | 添加注释`<br>` src/main/java/... | 4 ++--`<br>` 1 file changed, 2 insertions(+), 2 deletions(-)`<br>2. 上传文件并调用接口 |
| **预期结果** | 1. 成功解析出 1 条 commit<br>2. author="yansonaa"，date=2026-07-05 07:48:16<br>3. additions=2，deletions=2，files_changed=1 |
| **优先级** | P0 |
| **执行状态** | ✅ 通过 |

### TC-GL-003：CSV 格式文件上传

| 项目 | 内容 |
|------|------|
| **用例名称** | CSV 格式文件上传解析 |
| **前置条件** | 同 TC-GL-001 |
| **测试步骤** | 1. 准备 CSV 文件，包含 header：`commit_hash,author,date,message,additions,deletions,files_changed`<br>2. 上传文件 |
| **预期结果** | 1. 根据文件扩展名 `.csv` 自动走 CSV 解析分支<br>2. 所有行正确解析入库 |
| **优先级** | P0 |
| **执行状态** | ✅ 通过 |

### TC-GL-004：空文件上传

| 项目 | 内容 |
|------|------|
| **用例名称** | 空文件或无法解析的文件上传 |
| **前置条件** | 同 TC-GL-001 |
| **测试步骤** | 1. 准备空 txt 文件<br>2. 上传并调用接口 |
| **预期结果** | 1. HTTP 400 返回 `{"detail":"未能解析到任何提交记录"}`<br>2. 数据库无新增记录 |
| **优先级** | P0 |
| **执行状态** | ✅ 通过 |

### TC-GL-005：大文件上传（5000+ commits）

| 项目 | 内容 |
|------|------|
| **用例名称** | 大文件批量导入性能测试 |
| **前置条件** | 同 TC-GL-001 |
| **测试步骤** | 1. 生成包含 5000 条 commit 的标准 git log 文件<br>2. 上传并计时 |
| **预期结果** | 1. 成功导入 5000 条记录<br>2. 耗时 < 5 秒<br>3. 内存占用无显著增长 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

### TC-GL-006：中文文件名和路径处理

| 项目 | 内容 |
|------|------|
| **用例名称** | 中文文件名和消息内容解析 |
| **前置条件** | 同 TC-GL-001 |
| **测试步骤** | 1. 文件名：`git日志.txt`<br>2. 文件内容包含中文 author 和 message |
| **预期结果** | 1. 文件名正常处理<br>2. 中文内容无乱码正确入库<br>3. API 返回中文正常 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

---

## 3. 数据导入 - JSON 粘贴测试用例

### TC-JS-001：标准 JSON 数组导入

| 项目 | 内容 |
|------|------|
| **用例名称** | 标准字段名 JSON 导入 |
| **前置条件** | 数据库已清空 |
| **测试步骤** | 1. 粘贴 JSON：`[{ "commit_hash": "abc...", "author": "张三", "date": "2024-01-01T00:00:00", "additions": 10, "deletions": 2, "files_changed": 3 }]`<br>2. 点击"导入 JSON" |
| **预期结果** | 1. HTTP 200，返回 `{"message":"成功导入 1 条提交记录","count":1}`<br>2. 数据库新增 1 条记录 |
| **优先级** | P0 |
| **执行状态** | ✅ 通过 |

### TC-JS-002：字段别名映射（hash → commit_hash）

| 项目 | 内容 |
|------|------|
| **用例名称** | 字段别名自动映射导入 |
| **前置条件** | 数据库已清空 |
| **测试步骤** | 1. 粘贴 JSON：`[{ "hash": "abc...", "author": "张三", "date": "2024-01-01T00:00:00" }]`<br>2. 点击导入 |
| **预期结果** | 1. 前端 `normalizeCommit` 自动将 `hash` 映射为 `commit_hash`<br>2. 导入成功，数据库记录正确 |
| **优先级** | P0 |
| **执行状态** | ✅ 通过 |

### TC-JS-003：嵌套结构（GitHub API 格式）

| 项目 | 内容 |
|------|------|
| **用例名称** | GitHub API 嵌套 JSON 导入 |
| **前置条件** | 数据库已清空 |
| **测试步骤** | 1. 粘贴 JSON：`[{ "sha": "abc...", "commit": { "author": { "name": "张三", "date": "2024-01-01T00:00:00Z" }, "message": "fix" }, "stats": { "additions": 10, "deletions": 2 } }]`<br>2. 点击导入 |
| **预期结果** | 1. `normalizeCommit` 正确提取 `commit.author.name`、`commit.message`、`stats.additions`<br>2. 导入成功 |
| **优先级** | P0 |
| **执行状态** | ✅ 通过 |

### TC-JS-004：null 值处理

| 项目 | 内容 |
|------|------|
| **用例名称** | JSON 中包含 null 数值字段 |
| **前置条件** | 数据库已清空 |
| **测试步骤** | 1. 粘贴 JSON：`[{ "commit_hash": "abc...", "author": "张三", "date": "2024-01-01T00:00:00", "additions": 0, "deletions": 0, "files_changed": null }]`<br>2. 点击导入 |
| **预期结果** | 1. Pydantic validator `default_zero_for_none` 将 `null` 转为 `0`<br>2. HTTP 200，导入成功，无 422 错误 |
| **优先级** | P0 |
| **执行状态** | ✅ 通过 |

### TC-JS-005：外层包裹对象

| 项目 | 内容 |
|------|------|
| **用例名称** | { "commits": [...] } 格式导入 |
| **前置条件** | 数据库已清空 |
| **测试步骤** | 1. 粘贴 JSON：`{ "commits": [{ "commit_hash": "abc...", "author": "张三", "date": "2024-01-01T00:00:00" }] }`<br>2. 点击导入 |
| **预期结果** | 1. 前端 `handleJsonUpload` 自动提取 `parsed.commits`<br>2. 成功导入 1 条记录 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

### TC-JS-006：非法 JSON 格式

| 项目 | 内容 |
|------|------|
| **用例名称** | 非法 JSON 粘贴导入 |
| **前置条件** | 无 |
| **测试步骤** | 1. 粘贴非法文本：`abc123`<br>2. 点击导入 |
| **预期结果** | 1. 前端 `JSON.parse` 抛出异常<br>2. 错误提示：`解析失败` 或 `Unexpected token...`<br>3. 不调用后端接口 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

### TC-JS-007：空文本导入

| 项目 | 内容 |
|------|------|
| **用例名称** | 未填写 JSON 点击导入按钮 |
| **前置条件** | 无 |
| **测试步骤** | 1. 不输入任何内容，直接点击"导入 JSON" |
| **预期结果** | 1. 弹出 `alert('请先输入Git Log JSON 数据再进行导入JSON数据')`<br>2. 不调用后端接口 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

---

## 4. 数据导入 - 仓库同步测试用例

### TC-SYNC-001：本地 Git 仓库同步

| 项目 | 内容 |
|------|------|
| **用例名称** | 本地 Git 仓库配置与同步 |
| **前置条件** | 1. 本地存在有效的 Git 仓库<br>2. 后端有读写权限 |
| **测试步骤** | 1. POST `/api/sync/configs` 创建配置：`{ "name": "测试仓库", "repo_type": "local", "local_path": "E:/projects/test-repo", "branch": "main" }`<br>2. POST `/api/sync/sync/1` 触发同步 |
| **预期结果** | 1. 配置创建成功，返回 `id`<br>2. 同步成功，返回 `{"success": true, "imported_count": N}`<br>3. 数据库新增对应提交记录 |
| **优先级** | P0 |
| **执行状态** | ✅ 通过 |

### TC-SYNC-002：GitHub 远程仓库同步

| 项目 | 内容 |
|------|------|
| **用例名称** | GitHub 仓库 Token 同步 |
| **前置条件** | 1. 有效的 GitHub Personal Access Token<br>2. 有访问权限的公开/私有仓库 |
| **测试步骤** | 1. 创建配置：`{ "name": "GitHub测试", "repo_type": "github", "remote_url": "https://github.com/user/repo", "access_token": "ghp_xxx", "branch": "main" }`<br>2. 触发同步 |
| **预期结果** | 1. PyGithub 成功连接<br>2. 拉取最近 90 天提交（默认 5000 条上限）<br>3. 数据正确解析入库 |
| **优先级** | P0 |
| **执行状态** | ✅ 通过 |

### TC-SYNC-003：GitLab 远程仓库同步

| 项目 | 内容 |
|------|------|
| **用例名称** | GitLab 仓库 Token 同步 |
| **前置条件** | 1. 有效的 GitLab Private Token<br>2. 有访问权限的仓库 |
| **测试步骤** | 1. 创建配置：`repo_type: "gitlab"`<br>2. 触发同步 |
| **预期结果** | 1. requests 成功调用 GitLab API<br>2. 数据正确解析入库 |
| **优先级** | P0 |
| **执行状态** | ✅ 通过 |

### TC-SYNC-004：全仓库同步

| 项目 | 内容 |
|------|------|
| **用例名称** | 同步所有活跃仓库 |
| **前置条件** | 已创建 3 个仓库配置（2 active + 1 inactive） |
| **测试步骤** | 1. POST `/api/sync/sync-all` |
| **预期结果** | 1. 只同步 `is_active=true` 的 2 个仓库<br>2. 返回每个仓库的同步结果数组<br>3. 数据库新增所有仓库的提交 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

### TC-SYNC-005：分支切换

| 项目 | 内容 |
|------|------|
| **用例名称** | 同步非默认分支 |
| **前置条件** | 本地仓库有 `develop` 分支 |
| **测试步骤** | 1. 创建配置，`branch: "develop"`<br>2. 触发同步 |
| **预期结果** | 1. 拉取 `develop` 分支的提交历史<br>2. 数据正确入库 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

---

## 5. 数据库与去重测试用例

### TC-DB-001：精确重复去重

| 项目 | 内容 |
|------|------|
| **用例名称** | 相同 commit_hash 重复导入 |
| **前置条件** | 数据库已存在 `commit_hash = abc123...` |
| **测试步骤** | 1. 再次导入相同 hash 的 JSON 数据<br>2. 调用 `create_commits_bulk` |
| **预期结果** | 1. `create_commits_bulk` 返回 `0`（无新记录插入）<br>2. 数据库中该 hash 记录仍为 1 条<br>3. 无 500 唯一约束冲突错误 |
| **优先级** | P0 |
| **执行状态** | ✅ 通过 |

### TC-DB-002：短 hash → 完整 hash 去重

| 项目 | 内容 |
|------|------|
| **用例名称** | 短 hash 导入时完整 hash 已存在 |
| **前置条件** | 数据库已存在完整 hash `eb7fe64f951...` |
| **测试步骤** | 1. 导入 JSON：`[{ "hash": "eb7fe64", "author": "yansonaa", "date": "2026-07-05" }]` |
| **预期结果** | 1. `create_commits_bulk` 检测到短 hash 前缀匹配完整 hash<br>2. 返回 `0`，跳过插入<br>3. 数据库仍为 1 条记录 |
| **优先级** | P0 |
| **执行状态** | ✅ 通过 |

### TC-DB-003：完整 hash → 短 hash 去重

| 项目 | 内容 |
|------|------|
| **用例名称** | 完整 hash 导入时短 hash 已存在 |
| **前置条件** | 数据库已存在短 hash `eb7fe64` |
| **测试步骤** | 1. 导入 JSON：`[{ "commit_hash": "eb7fe64f951...", "author": "yansonaa", "date": "2026-07-05" }]` |
| **预期结果** | 1. `create_commits_bulk` 检测到完整 hash 以短 hash 开头<br>2. 跳过插入或删除短 hash 后插入完整 hash<br>3. 最终数据库中该 commit 只有 1 条记录（完整 hash） |
| **优先级** | P0 |
| **执行状态** | ✅ 通过 |

### TC-DB-004：BOM 字符清理

| 项目 | 内容 |
|------|------|
| **用例名称** | 带 BOM 前缀的 commit_hash 导入 |
| **前置条件** | 数据库已存在 `28c0e05b925...` |
| **测试步骤** | 1. 导入 hash 为 `﻿28c0e05`（带 BOM）的数据 |
| **预期结果** | 1. `lstrip('\\ufeff')` 清理 BOM 字符<br>2. 清理后与前缀匹配，跳过插入 |
| **优先级** | P0 |
| **执行状态** | ✅ 通过 |

### TC-DB-005：清空数据

| 项目 | 内容 |
|------|------|
| **用例名称** | 清空所有提交记录 |
| **前置条件** | 数据库已存在 N 条记录 |
| **测试步骤** | 1. DELETE `/api/commits/all` |
| **预期结果** | 1. 返回 `{"message": "已删除 N 条提交记录"}`<br>2. `commits` 表记录数为 0 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

### TC-DB-006：并发导入无冲突

| 项目 | 内容 |
|------|------|
| **用例名称** | 批量导入时并发查询稳定性 |
| **前置条件** | 数据库已有 1000 条记录 |
| **测试步骤** | 1. 同时发起 3 个包含 100 条记录的批量导入请求 |
| **预期结果** | 1. 所有请求成功返回<br>2. 无 SQLite 锁冲突错误<br>3. 最终数据库记录数正确 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

---

## 6. 统计 API 测试用例

### TC-API-001：KPI 概览（无过滤）

| 项目 | 内容 |
|------|------|
| **用例名称** | 默认日期范围 KPI 查询 |
| **前置条件** | 数据库有 30 天内提交数据 |
| **测试步骤** | 1. GET `/api/stats/kpi` |
| **预期结果** | 1. `total_commits` >= 0<br>2. `active_developers` >= 0<br>3. `period_days` = 30（默认） |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

### TC-API-002：KPI 概览（日期过滤）

| 项目 | 内容 |
|------|------|
| **用例名称** | 自定义日期范围 KPI 查询 |
| **前置条件** | 数据库有 2026-06-01 至 2026-07-05 的数据 |
| **测试步骤** | 1. GET `/api/stats/kpi?start_date=2026-06-01T00:00:00&end_date=2026-07-05T23:59:59` |
| **预期结果** | 1. 只统计该日期范围内的提交<br>2. `period_days` = 35 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

### TC-API-003：趋势图（下降检测）

| 项目 | 内容 |
|------|------|
| **用例名称** | 趋势下降自动检测 |
| **前置条件** | 前 7 天提交数 = 70，最近 7 天提交数 = 20 |
| **测试步骤** | 1. GET `/api/stats/trend` |
| **预期结果** | 1. `has_decline` = true<br>2. `decline_dates` 包含最近 7 天的日期 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

### TC-API-004：热力图（异常标记）

| 项目 | 内容 |
|------|------|
| **用例名称** | 深夜提交异常检测 |
| **前置条件** | 数据库有周三凌晨 3 点的 8 次提交，max_count=10 |
| **测试步骤** | 1. GET `/api/stats/heatmap` |
| **预期结果** | 1. `anomalies` 非空<br>2. `anomalies[0].type` = "late_night_batch"<br>3. `anomalies[0].description` 包含"周三"和"8 次提交" |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

### TC-API-005：搜索过滤（作者名）

| 项目 | 内容 |
|------|------|
| **用例名称** | 按作者名搜索统计 |
| **前置条件** | 数据库有多位作者数据 |
| **测试步骤** | 1. GET `/api/stats/kpi?search=yansonaa` |
| **预期结果** | 1. `total_commits` 只统计 author 包含 "yansonaa" 的记录<br>2. 其他作者的提交不计入 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

### TC-API-006：搜索过滤（Commit Message）

| 项目 | 内容 |
|------|------|
| **用例名称** | 按 Commit Message 搜索统计 |
| **前置条件** | 数据库有包含 "fix" 的提交 |
| **测试步骤** | 1. GET `/api/stats/kpi?search=fix` |
| **预期结果** | 1. 只统计 message 包含 "fix" 的提交 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

### TC-API-007：雷达图（无作者参数）

| 项目 | 内容 |
|------|------|
| **用例名称** | 全作者雷达图 |
| **前置条件** | 数据库有 5 位作者数据 |
| **测试步骤** | 1. GET `/api/stats/radar` |
| **预期结果** | 1. 返回 5 个作者的雷达数据<br>2. 每个作者有 5 个维度 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

### TC-API-008：代码量统计（Top 15）

| 项目 | 内容 |
|------|------|
| **用例名称** | 代码量统计作者上限 |
| **前置条件** | 数据库有 20 位作者 |
| **测试步骤** | 1. GET `/api/stats/code` |
| **预期结果** | 1. 返回结果数量 <= 15（Top 15）<br>2. 按净增行数降序排列 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

---

## 7. 数据导出测试用例

### TC-EXP-001：提交明细 CSV 导出

| 项目 | 内容 |
|------|------|
| **用例名称** | 导出提交记录 CSV |
| **前置条件** | 数据库有提交记录 |
| **测试步骤** | 1. GET `/api/export/csv` |
| **预期结果** | 1. Content-Type: text/csv<br>2. 文件包含所有 commit 字段<br>3. 中文字符无乱码 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

### TC-EXP-002：成员统计 CSV 导出

| 项目 | 内容 |
|------|------|
| **用例名称** | 导出成员统计 CSV |
| **前置条件** | 数据库有提交记录 |
| **测试步骤** | 1. GET `/api/export/members/csv` |
| **预期结果** | 1. 文件包含 author、commits、additions、deletions、net、avg_per_commit<br>2. 按 commits 降序排列 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

### TC-EXP-003：导出日期范围过滤

| 项目 | 内容 |
|------|------|
| **用例名称** | 按日期范围导出 |
| **前置条件** | 数据库有跨月数据 |
| **测试步骤** | 1. GET `/api/export/csv?start_date=2026-06-01&end_date=2026-06-30` |
| **预期结果** | 1. 只导出 6 月份的提交记录 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

---

## 8. 前端交互测试用例

### TC-UI-001：搜索框防抖

| 项目 | 内容 |
|------|------|
| **用例名称** | 搜索输入防抖触发 |
| **前置条件** | 页面已加载，仪表盘正常显示 |
| **测试步骤** | 1. 在搜索框输入 "张"<br>2. 等待 200ms<br>3. 继续输入 "三" |
| **预期结果** | 1. 输入 "张" 后 200ms 触发一次 API 请求<br>2. 输入 "三" 后 200ms 再次触发请求<br>3. 无中间过程的频繁请求 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

### TC-UI-002：日期范围选择

| 项目 | 内容 |
|------|------|
| **用例名称** | 日期范围选择后自动刷新 |
| **前置条件** | 页面已加载 |
| **测试步骤** | 1. 选择开始日期：2026-06-01<br>2. 选择结束日期：2026-06-30 |
| **预期结果** | 1. 日期选择后 `useEffect` 触发<br>2. 并行调用 6 个统计接口<br>3. 所有图表和数据更新 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

### TC-UI-003：热力图 tooltip

| 项目 | 内容 |
|------|------|
| **用例名称** | 热力图 tooltip 正确显示 |
| **前置条件** | 热力图有数据 |
| **测试步骤** | 1. 鼠标悬停在热力图任意单元格 |
| **预期结果** | 1. Tooltip 显示格式：`周二 14:00\n提交次数: 5`<br>2. 无 `undefined` 出现 |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

### TC-UI-004：导入弹框关闭

| 项目 | 内容 |
|------|------|
| **用例名称** | 导入成功后弹框关闭和数据刷新 |
| **前置条件** | 导入弹窗已打开 |
| **测试步骤** | 1. 选择文件并上传成功 |
| **预期结果** | 1. 显示 `alert('上传成功...')`<br>2. 弹框关闭<br>3. 仪表盘自动刷新（`onSuccess` 触发 `fetchData`） |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

### TC-UI-005：响应式布局（1314×832）

| 项目 | 内容 |
|------|------|
| **用例名称** | 中等分辨率下布局正常 |
| **前置条件** | 浏览器窗口大小 1314×832 |
| **测试步骤** | 1. 调整浏览器窗口至 1314×832<br>2. 观察控制面板布局 |
| **预期结果** | 1. 4 列 grid 使用 `1fr_1.2fr_0.8fr_1fr` 比例<br>2. 日期输入框完整显示，日历图标不被遮挡<br>3. 搜索框宽度不超过 220px |
| **优先级** | P1 |
| **执行状态** | ✅ 通过 |

### TC-UI-006：打印报表

| 项目 | 内容 |
|------|------|
| **用例名称** | 打印报表按钮功能 |
| **前置条件** | 页面已加载数据 |
| **测试步骤** | 1. 点击"打印报表"按钮 |
| **预期结果** | 1. 调用 `window.print()`<br>2. 控制面板隐藏（`no-print` 类）<br>3. 图表适配 A4 尺寸 |
| **优先级** | P2 |
| **执行状态** | ✅ 通过 |

---

## 9. 模拟数据测试用例

### TC-MOCK-001：模拟数据生成

| 项目 | 内容 |
|------|------|
| **用例名称** | 一键生成模拟数据 |
| **前置条件** | 数据库已清空 |
| **测试步骤** | 1. POST `/api/mock/generate?days=90&total_commits=2000` |
| **预期结果** | 1. 返回成功<br>2. 数据库新增约 2000 条记录<br>3. 覆盖 90 天日期范围<br>4. 包含 25 个不同作者 |
| **优先级** | P2 |
| **执行状态** | ✅ 通过 |

### TC-MOCK-002：模拟数据覆盖

| 项目 | 内容 |
|------|------|
| **用例名称** | 重新生成模拟数据清空旧数据 |
| **前置条件** | 数据库已有真实数据 100 条 |
| **测试步骤** | 1. 再次调用模拟数据生成接口 |
| **预期结果** | 1. 旧数据被清空<br>2. 新数据插入成功<br>3. 数据库最终只有 2000 条模拟数据 |
| **优先级** | P2 |
| **执行状态** | ✅ 通过 |

---

## 10. 测试环境信息

| 项目 | 配置 |
|------|------|
| **操作系统** | Windows 10 Home China (19045) |
| **Python 版本** | 3.7.8 |
| **Node.js 版本** | 18.x |
| **浏览器** | Google Chrome 1314×832 分辨率 |
| **后端端口** | 8000 |
| **前端端口** | 3000 (Vite dev server) |
| **数据库** | SQLite 3.x (backend/app/data/codepulse.db) |
| **测试数据** | git-log.txt (3 commits), git-log.json (5 commits), mock data (2000 commits) |

---

*本文档由 Claude Code 基于 CodePulse 项目功能分析生成，涵盖 46 条测试用例，覆盖核心功能、边界条件和异常场景。*